---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: concoursepipelines.concourse.concourse-ci.org
spec:
  group: concourse.concourse-ci.org
  names:
    kind: ConcoursePipeline
    listKind: ConcoursePipelineList
    plural: concoursepipelines
    singular: concoursepipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.teamRef.name
      name: Team
      type: string
    - jsonPath: .spec.pipelineName
      name: Pipeline
      type: string
    - jsonPath: .status.paused
      name: Paused
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConcoursePipeline manages a Concourse pipeline configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConcoursePipelineSpec defines the desired state of ConcoursePipeline.
            properties:
              config:
                description: Config defines the pipeline configuration source.
                properties:
                  configMapRef:
                    description: ConfigMapRef references a ConfigMap key containing
                      the pipeline YAML.
                    properties:
                      key:
                        description: Key within the ConfigMap.
                        type: string
                      name:
                        description: Name of the ConfigMap.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  inline:
                    description: Inline is the pipeline YAML embedded directly in
                      the spec.
                    type: string
                type: object
              exposed:
                description: Exposed makes the pipeline publicly visible without authentication.
                type: boolean
              paused:
                description: Paused sets the pipeline pause state in Concourse.
                type: boolean
              pipelineName:
                description: PipelineName is the name in Concourse. Defaults to metadata.name.
                type: string
              teamRef:
                description: TeamRef references the ConcourseTeam this pipeline belongs
                  to.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              vars:
                description: Vars are pipeline variables passed at set-pipeline time.
                items:
                  description: PipelineVar defines a variable passed to fly set-pipeline
                    -v/-l.
                  properties:
                    name:
                      description: Name of the variable.
                      type: string
                    value:
                      description: Value is the plain-text value. Mutually exclusive
                        with ValueFrom.
                      type: string
                    valueFrom:
                      description: ValueFrom sources the value from a Secret. Mutually
                        exclusive with Value.
                      properties:
                        key:
                          description: Key within the Secret.
                          type: string
                        name:
                          description: Name of the Secret.
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  required:
                  - name
                  type: object
                type: array
            required:
            - config
            - teamRef
            type: object
          status:
            description: ConcoursePipelineStatus defines the observed state of ConcoursePipeline.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: ConfigHash is the SHA-256 hash of the last successfully
                  applied pipeline YAML.
                type: string
              exposed:
                description: Exposed reflects the actual exposed state in Concourse.
                type: boolean
              observedGeneration:
                description: ObservedGeneration is the last generation that was reconciled.
                format: int64
                type: integer
              paused:
                description: Paused reflects the actual pause state in Concourse.
                type: boolean
              pipelineID:
                description: PipelineID is the Concourse-assigned pipeline ID.
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
